// =============================================================================
// duel.cfg — Duel server
// =============================================================================
exec configs/_base.cfg

set sv_defaultmap "wfdm1"                 // no default in duel.cfg; update as needed
set g_votable_gametypes "duel"
set g_disable_vote_shuffle "1"        // no teams in duel
set g_disable_vote_allow_uneven "1"   // 1v1 only
